# Frontend

Vite + React minimal scaffold.

Quick start

Windows (cmd):

```bat
cd frontend
npm install
npm run dev
```

If the frontend needs to call the backend API, a dev proxy is configured so requests to `/api` are forwarded to `http://localhost:5000`.

