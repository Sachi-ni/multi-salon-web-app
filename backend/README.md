# Backend

Express + Mongoose skeleton for the Multi-salon app.

Quick start

Windows (cmd):

```bat
cd backend
npm install
copy .env.example .env
npm run dev
```

Environment: set `MONGO_URI` and `JWT_SECRET` in `.env`.
